== Choose Your Own Adventure ==

ajax_record.js.erb is javascript file in which i can use javascript with ruby syntax. It's needed because ajax_record function in adventure controller will return the result to this file. both name will be same. e.g [ajax_record] and [ajax_record.js.erb]. the function ajax_record return the data to ajax_record.js.erb and then it's shows record on home page.
